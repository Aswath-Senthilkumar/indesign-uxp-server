{
    "$schema_note": "Per-template manifest. The render service scans template-manifests/<workflow>/<NAME>/manifest.json and aggregates all entries. `file` is the .indd filename (no directory) — the service resolves it via TEMPLATES_DIR. Add a new template by (1) dropping its .indd into TEMPLATES_DIR (out of repo), (2) creating a sibling folder here with a manifest.json of this shape, (3) restarting the service.",
    "id": "18-tile-price-status",
    "label": "18 Tiles Template with defaults along with Price and Status fields",
    "file": "18_Tile_Price_Status.indd",
    "grid": {
        "cols": 3
    },
    "tile_fields": [
        {
            "field": "address",
            "frame_pattern": "tile_{N}_address",
            "type": "text",
            "required": true
        },
        {
            "field": "city_state",
            "frame_pattern": "tile_{N}_city_state",
            "type": "text",
            "required": true
        },
        {
            "field": "sf_ac",
            "frame_pattern": "tile_{N}_sf_ac",
            "type": "text",
            "required": true,
            "format": "±{building_sf:number} SF | ±{land_area:number_2dp} AC"
        },
        {
            "field": "price",
            "frame_pattern": "tile_{N}_price",
            "type": "text",
            "required": false,
            "rule": "price_line_v1"
        },
        {
            "field": "status",
            "frame_pattern": "tile_{N}_status",
            "type": "text",
            "required": false,
            "rule": "status_badge_v1"
        },
        {
            "field": "photo",
            "frame_pattern": "tile_{N}_photo",
            "type": "image",
            "required": true,
            "fit": "fillProportionally"
        }
    ],
    "page_fields": [
        {
            "field": "page_1_title",
            "frame": "page_1_title",
            "type": "text",
            "editable": true
        },
        {
            "field": "page_2_title",
            "frame": "page_2_title",
            "type": "text",
            "editable": true
        },
        {
            "field": "tagline",
            "frame": "page_tagline",
            "type": "text",
            "editable": true
        }
    ],
    "static_frames_note": "18 tiles split 3 cols x 3 rows per page across 2 pages. Page-level frames: page_1_title (page 1 only) and page_2_title (page 2 only) — separate inputs since the two pages carry different titles. page_tagline appears once on each page and shares a name; the bridge fans the override to all matching frames so the tagline is identical on both pages. Static frames (agent name plates, contact lines, branding/logo, decorative shapes, tile background rects named tile_N) are managed in InDesign directly."
}
