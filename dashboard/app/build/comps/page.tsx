// Stage 5.1 placeholder — real comps-picker UI lands in Stage 5.3.

export default function BuildCompsPage() {
    return (
        <section>
            <h1 className="text-2xl font-semibold tracking-tight">Pick comps</h1>
            <p className="mt-2 text-sm text-muted-foreground">
                Stage 5.1 placeholder. The comp-picker UI is wired up in
                Stage 5.3.
            </p>
        </section>
    );
}
