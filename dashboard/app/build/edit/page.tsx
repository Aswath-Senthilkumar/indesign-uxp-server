// Stage 5.1 placeholder — split-screen edit + render lands in Stage 5.4.

export default function BuildEditPage() {
    return (
        <section>
            <h1 className="text-2xl font-semibold tracking-tight">
                Edit & render
            </h1>
            <p className="mt-2 text-sm text-muted-foreground">
                Stage 5.1 placeholder. Split-screen edit + inline preview ships
                in Stage 5.4.
            </p>
        </section>
    );
}
